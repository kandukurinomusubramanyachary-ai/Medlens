English OCR runs locally when `eng.traineddata` is installed here and `OCR_ENABLED=true`.

Download the official Tesseract English model with `node scripts/setup-ocr.mjs`.
The model is application data; uploaded medical files are never written to this directory.
